import { NewAppointmentComponent } from './new-appointment.component';

describe('NewAppointmentComponent', () => {
  let component: NewAppointmentComponent;

  beforeEach(() => {
    component = new NewAppointmentComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
